import 'package:flutter/material.dart';

abstract class AppColors {
  AppColors._();

  // Background
  static const Color background = Color(0xFF022C43);

  // Text
  static const Color textPrimary = Colors.white;
  static const Color textSecondary = Colors.white70;
  static const Color textHint = Colors.white54;

  // Accent
  static const Color accentPrimary = Color(0xFF22D3EE);
  static const Color accentSecondary = Color(0xFF0EA5E9);

  // Glass / Card
  static const Color glass = Colors.white12;
}
